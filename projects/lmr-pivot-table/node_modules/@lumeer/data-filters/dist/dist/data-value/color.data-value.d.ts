import { DataValue } from './data-value';
import { ConditionType, ConditionValue, ColorConstraintConfig } from '../model';
export declare class ColorDataValue implements DataValue {
    readonly value: any;
    readonly config: ColorConstraintConfig;
    readonly inputValue?: string;
    readonly hexCode: string;
    readonly numberCode: number;
    constructor(value: any, config: ColorConstraintConfig, inputValue?: string);
    format(): string;
    preview(): string;
    title(): string;
    editValue(): string;
    serialize(): any;
    isValid(ignoreConfig?: boolean): boolean;
    increment(): ColorDataValue;
    decrement(): ColorDataValue;
    compareTo(otherValue: ColorDataValue): number;
    copy(newValue?: any): ColorDataValue;
    parseInput(inputValue: string): ColorDataValue;
    meetCondition(condition: ConditionType, values: ConditionValue[]): boolean;
    meetFullTexts(fulltexts: string[]): boolean;
    readableColor(): string;
    valueByCondition(condition: ConditionType, values: ConditionValue[]): any;
}
