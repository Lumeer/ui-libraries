import { DurationDataValue } from '../data-value';
import { Constraint } from './constraint';
import { ConstraintType, ConditionType, DurationConstraintConfig } from '../model';
import { ConstraintData } from './constraint-data';
export declare class DurationConstraint implements Constraint {
    readonly config: DurationConstraintConfig;
    readonly type = ConstraintType.Duration;
    readonly isTextRepresentation = true;
    readonly allowEditFunction = true;
    constructor(config: DurationConstraintConfig);
    createDataValue(value: any, constraintData: ConstraintData): DurationDataValue;
    createInputDataValue(inputValue: string, value: any, constraintData: ConstraintData): DurationDataValue;
    conditions(): ConditionType[];
    avg(values: any[], onlyNumeric?: boolean): any;
    max(values: any[], onlyNumeric?: boolean): any;
    median(values: any[], onlyNumeric?: boolean): any;
    min(values: any[], onlyNumeric?: boolean): any;
    sum(values: any[], onlyNumeric?: boolean): any;
    unique(values: any[]): any;
    count(values: any[]): number;
    filterInvalidValues<T extends {
        data: Record<string, any>;
    }>(objects: T[], attributeId: string): Set<any>;
}
