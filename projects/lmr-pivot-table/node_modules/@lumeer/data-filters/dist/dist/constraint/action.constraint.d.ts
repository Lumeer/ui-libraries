import { Constraint } from './constraint';
import { ActionDataValue } from '../data-value';
import { ConditionType, ActionConstraintConfig, ConstraintType } from '../model';
export declare class ActionConstraint implements Constraint {
    readonly config: ActionConstraintConfig;
    readonly type = ConstraintType.Action;
    readonly isDirectlyEditable = true;
    constructor(config: ActionConstraintConfig);
    createDataValue(value: any): ActionDataValue;
    createInputDataValue(inputValue: string, value: any): ActionDataValue;
    conditions(): ConditionType[];
    avg(values: any[], onlyNumeric?: boolean): any;
    max(values: any[], onlyNumeric?: boolean): any;
    median(values: any[], onlyNumeric?: boolean): any;
    min(values: any[], onlyNumeric?: boolean): any;
    sum(values: any[], onlyNumeric?: boolean): any;
    unique(values: any[]): any;
    count(values: any[]): number;
    filterInvalidValues<T extends {
        data: Record<string, any>;
    }>(objects: T[], attributeId: string): Set<any>;
}
