import { TeamDataValue } from '../data-value';
import { Constraint } from './constraint';
import { ConditionType, ConstraintType, TeamConstraintConfig } from '../model';
import { ConstraintData } from './constraint-data';
export declare class TeamConstraint implements Constraint {
    readonly config: TeamConstraintConfig;
    readonly type = ConstraintType.Team;
    readonly allowEditFunction = true;
    constructor(config: TeamConstraintConfig);
    createDataValue(value: any, constraintData: ConstraintData): TeamDataValue;
    createInputDataValue(inputValue: string, value: any, constraintData: ConstraintData): TeamDataValue;
    conditions(): ConditionType[];
    avg(values: any[], onlyNumeric?: boolean): any;
    max(values: any[], onlyNumeric?: boolean): any;
    median(values: any[], onlyNumeric?: boolean): any;
    min(values: any[], onlyNumeric?: boolean): any;
    sum(values: any[], onlyNumeric?: boolean): any;
    unique(values: any[]): any;
    count(values: any[]): number;
    filterInvalidValues<T extends {
        data: Record<string, any>;
    }>(objects: T[], attributeId: string, constraintData: ConstraintData): Set<any>;
}
