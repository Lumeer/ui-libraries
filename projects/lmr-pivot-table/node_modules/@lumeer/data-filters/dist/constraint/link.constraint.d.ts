import { Constraint } from './constraint';
import { LinkDataValue } from '../data-value';
import { ConditionType, LinkConstraintConfig, ConstraintType } from '../model';
export declare class LinkConstraint implements Constraint {
    readonly config: LinkConstraintConfig;
    readonly type = ConstraintType.Link;
    readonly allowEditFunction = true;
    constructor(config: LinkConstraintConfig);
    createDataValue(value: any): LinkDataValue;
    createInputDataValue(inputValue: string, value: any): LinkDataValue;
    conditions(): ConditionType[];
    avg(values: any[], onlyNumeric?: boolean): any;
    max(values: any[], onlyNumeric?: boolean): any;
    median(values: any[], onlyNumeric?: boolean): any;
    min(values: any[], onlyNumeric?: boolean): any;
    sum(values: any[], onlyNumeric?: boolean): any;
    unique(values: any[]): any;
    count(values: any[]): number;
    filterInvalidValues<T extends {
        data: Record<string, any>;
    }>(objects: T[], attributeId: string): Set<any>;
}
