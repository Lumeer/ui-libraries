import { MapCoordinates, CoordinatesFormat } from '../model';
export declare function parseCoordinates(value: any): MapCoordinates;
export declare function formatCoordinates(coordinates: MapCoordinates, format: CoordinatesFormat, precision: number): string;
