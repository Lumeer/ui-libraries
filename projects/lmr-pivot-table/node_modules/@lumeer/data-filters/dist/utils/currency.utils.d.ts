import numbro from 'numbro';
import NumbroLanguage = numbro.NumbroLanguage;
import { LanguageTag } from '../model';
import { CurrencyData } from '../constraint';
export interface Currency {
    symbol: string;
    code: string;
    placement: CurrencyPlacement;
    spaceSeparated: boolean;
    decimalSeparator: string;
    thousandSeparator: string;
}
export declare enum CurrencyPlacement {
    prefix = "prefix",
    suffix = "suffix"
}
export declare const currencies: Record<string, Currency>;
export declare function getNumbroLanguage(currencyLanguage: LanguageTag, locale: LanguageTag, data: CurrencyData): NumbroLanguage;
export declare function ordinal(num: number, data: CurrencyData): string;
