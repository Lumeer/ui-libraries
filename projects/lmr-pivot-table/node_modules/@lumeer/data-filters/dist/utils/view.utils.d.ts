import { Collection, View } from '../model';
export declare function getViewColor(view: View, collectionsMap: Record<string, Collection>): string;
export declare function defaultViewColorFromQuery(view: View, collectionsMap: Record<string, Collection>): string;
