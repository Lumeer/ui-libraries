import { DataValue } from './data-value';
import { ConditionType, ConditionValue, TextConstraintConfig } from '../model';
export declare class TextDataValue implements DataValue {
    readonly value: any;
    readonly config: TextConstraintConfig;
    readonly inputValue?: string;
    constructor(value: any, config: TextConstraintConfig, inputValue?: string);
    format(): string;
    preview(): string;
    title(): string;
    editValue(): string;
    serialize(): any;
    isValid(ignoreConfig?: boolean): boolean;
    increment(): TextDataValue;
    decrement(): TextDataValue;
    compareTo(otherValue: TextDataValue): number;
    copy(newValue?: any): TextDataValue;
    parseInput(inputValue: string): TextDataValue;
    meetCondition(condition: ConditionType, values: ConditionValue[]): boolean;
    meetFullTexts(fulltexts: string[]): boolean;
    valueByCondition(condition: ConditionType, values: ConditionValue[]): any;
}
