import { ConstraintData } from '../constraint';
import { DataValue } from './data-value';
import { ConditionType, ConditionValue, View, ViewConstraintConfig } from '../model';
export declare class ViewDataValue implements DataValue {
    readonly value: any;
    readonly config: ViewConstraintConfig;
    readonly constraintData: ConstraintData;
    readonly inputValue?: string;
    readonly views: View[];
    readonly validViews: View[];
    constructor(value: any, config: ViewConstraintConfig, constraintData: ConstraintData, inputValue?: string);
    private createViews;
    format(): string;
    preview(): string;
    title(): string;
    editValue(): string;
    serialize(): any;
    isValid(ignoreConfig?: boolean): boolean;
    increment(): ViewDataValue;
    decrement(): ViewDataValue;
    compareTo(otherValue: ViewDataValue): number;
    copy(newValue?: any): ViewDataValue;
    parseInput(inputValue: string): ViewDataValue;
    meetCondition(condition: ConditionType, values: ConditionValue[]): boolean;
    private mapQueryConditionValue;
    meetFullTexts(fulltexts: string[]): boolean;
    valueByCondition(condition: ConditionType, values: ConditionValue[]): any;
}
