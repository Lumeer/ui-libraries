import { DataValue } from './data-value';
import { ConditionType, ConditionValue, MapCoordinates, CoordinatesConstraintConfig } from '../model';
export declare class CoordinatesDataValue implements DataValue {
    readonly value: any;
    readonly config: CoordinatesConstraintConfig;
    readonly inputValue?: string;
    readonly coordinates: MapCoordinates;
    constructor(value: any, config: CoordinatesConstraintConfig, inputValue?: string);
    format(): string;
    preview(): string;
    title(): string;
    editValue(): string;
    serialize(): any;
    isValid(ignoreConfig?: boolean): boolean;
    increment(): CoordinatesDataValue;
    decrement(): CoordinatesDataValue;
    compareTo(otherValue: CoordinatesDataValue): number;
    copy(newValue?: any): CoordinatesDataValue;
    parseInput(inputValue: string): CoordinatesDataValue;
    meetCondition(condition: ConditionType, values: ConditionValue[]): boolean;
    meetFullTexts(fulltexts: string[]): boolean;
    valueByCondition(condition: ConditionType, values: ConditionValue[]): any;
}
